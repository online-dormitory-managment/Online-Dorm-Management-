{
  "name": "online-dorm-management",
  "private": true,
  "description": "Monorepo scripts — server and client live in /server and /client",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "seed:students": "npm run seed:students --prefix server",
    "seed:students:merge": "npm run seed:students:merge --prefix server",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --workspace client && mkdir -p dist && cp -rv client/dist/* dist/"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "optionalDependencies": {
    "@rollup/rollup-linux-x64-gnu": "4.54.0"
  }
}
